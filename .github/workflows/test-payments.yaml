name: test-payments

on: # when we do this workflow
  pull_request:
    paths:
      - "payments/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd payments && yarn && yarn test:ci
